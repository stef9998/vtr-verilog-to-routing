public enum VertexType {
    // vertex types in MUX are source node (SRCNODE), sink node (SINKNODE) and inter node (INTERNODE)
    SRCNODE,    // source nodes of the MUX
    SINKNODE,   // sink node of the MUX
    INTERNODE   // node between first and second stage in the MUX
}
